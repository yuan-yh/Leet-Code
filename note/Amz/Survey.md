## Yutika Yong / AWS DynamoDB: Technical Experience

C, C++ [Java, Scala, Go]:

Distributed Systems:

Compiler:

Computer Architecture:

Database Management:

Query Optimization/Query Processing:

### Response
- C, C++ [Java, Scala, Go]:

For C++ and Java, developed high-concurrency Spring Boot backend and Android apps in Java; built a thread-safe buffer pool, concurrent B+Tree, and query executors in C++.

- Distributed Systems:

Implemented a high-concurrency backend using Redis for session/gating and Kafka for asynchronous processing, preventing flash-sale overselling and ensuring stateless scaling through Nginx load balancing.

- Database Management:

Built a C++ buffer pool manager for page caching and replacement. Redesigned PostgreSQL schemas and optimized queries during internships, reducing latency across 50,000+ product records.

- Query Optimization/Query Processing:

Developed scan, join, and aggregation executors for a C++ relational engine. Improved production performance through SQL query optimization and strategic schema refactoring during internship.

## Yashvi Yu / Team 2: Technical Experience
C, C++ [Java, Scala, Go, Postgres]

Distributed systems

Computer architecture

Query Optimization/Query Processing

Database Management

Data Notebook

Jupyter

Marimo

### Response

- C, C++ [Java, Scala, Go, Postgres]:

For C++, Java, and Postgres: developed high-concurrency Spring Boot backends in Java; built a thread-safe buffer pool, concurrent B+Tree, and SQL executors in C++; redesigned PostgreSQL schemas and optimized queries.

- Distributed Systems:

Implemented a high-concurrency backend using Java and Redis for session/gating and Kafka for asynchronous processing, preventing flash-sale overselling and ensuring stateless scaling through Nginx load balancing.

- Database Management:

Built a C++ buffer pool manager for page caching and replacement. Redesigned PostgreSQL schemas and optimized queries during internships, reducing latency across 50,000+ product records.

- Query Optimization/Query Processing:

Developed scan, join, and aggregation executors for a C++ relational engine. Improved production performance through SQL query optimization and strategic schema refactoring during internship.

- Data Notebook / Jupyter:

Analyzed 10M+ hospital incident records in Jupyter notebooks using pandas, NLP, and matplotlib; built reproducible data pipelines delivering actionable insights on workplace violence prevention.