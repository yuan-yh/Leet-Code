# 

实战演练：API接口与Agent的整合​

![](https://s1-imfile.feishucdn.com/static-resource/v1/v3_00m7_8d5e6160-d954-4c3d-b8cc-a2f0af158abg~?image_size=72x72&cut_type=default-face&quality=&format=jpeg&sticker_format=.webp)看看看

Modified January 24

AI QuickView

Free trial

Generating AI QuickView…

This article discusses the integration of API interfaces and Agents in a practical project, focusing on the implementation of AI operation and maintenance interfaces in Go and Java. Key points include: ​

1.

Project framework: The project uses the goframe web framework, and you can refer to the Feishu document to understand the process from API definition to service provision. ​

2.

AI operation and maintenance interface: The AI operation and maintenance interface automatically queries active alarms and determines the root cause. The request method is POST /api/ai\_ops. ​

3.

Core implementation in Go: The key lies in the prompt design, guiding the large model to formulate plans. The code path is SuperBizAgent/internal/controller/chat/chat\_v1\_ai\_ops.go. ​

4.

Core implementation in Java: After building the API interface, call it directly and return the output. The method is a POST request to /ai\_ops in SSE streaming mode. ​

5.

Execution process in Java: Build Planner, Executor, and Supervisor Agents, and the Supervisor Agent invokes the task prompt to execute the analysis process. ​

6.

Timeout setting in Java: The SseEmitter has a timeout of 600,000 milliseconds (10 minutes) to handle potentially slow alarm analysis. ​

7.

Final report extraction: Extract the final report after the AI operation and maintenance orchestration is completed. ​

The content is generated by AI.

Expand

​

Code block​

Java

/\*\*​

\* AI 智能运维接口（SSE 流式模式）- 自动分析告警并生成运维报告​

\* 无需用户输入，自动执行告警分析流程​

\*/​

@PostMapping(value = "/ai\_ops", produces = "text/event-stream;charset=UTF-8")​

public SseEmitter aiOps() {​

SseEmitter emitter \= new SseEmitter(600000L); // 10分钟超时（告警分析可能较慢）​

executor.execute(() -> {​

try {​

// 调用 AiOpsService 执行分析流程​

Optional<OverAllState> overAllStateOptional = aiOpsService.executeAiOpsAnalysis(chatModel, toolCallbacks);​

OverAllState state \= overAllStateOptional.get();​

logger.info("AI Ops 编排完成，开始提取最终报告...");​

​

// 提取最终报告​

Optional<String> finalReportOptional = aiOpsService.extractFinalReport(state);​

// 输出最终报告​

if (finalReportOptional.isPresent()) {​

// 发送​

}​

}​

});​

return emitter;​

}​

​

public Optional<OverAllState> executeAiOpsAnalysis(DashScopeChatModel chatModel, ToolCallback\[\] toolCallbacks) throws GraphRunnerException {​

logger.info("开始执行 AI Ops 多 Agent 协作流程");​

// 构建 Planner 和 Executor Agent​

ReactAgent plannerAgent \= buildPlannerAgent(chatModel, toolCallbacks);​

ReactAgent executorAgent \= buildExecutorAgent(chatModel, toolCallbacks);​

// 构建 Supervisor Agent​

SupervisorAgent supervisorAgent \= SupervisorAgent.builder()​

.name("ai\_ops\_supervisor")​

.description("负责调度 Planner 与 Executor 的多 Agent 控制器")​

.model(chatModel)​

return supervisorAgent.invoke(taskPrompt);​

}​

​